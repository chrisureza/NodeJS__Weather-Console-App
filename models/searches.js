class Searches {
    history = ['Tegucigalpa', 'Madrid', 'San Jose'];

    constructor() {
        // Read file if exists
    }

    async searchCity(city = '') {
        // http request

        console.log(city);

        return [];
    }
}

module.exports = Searches;